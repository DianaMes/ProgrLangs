//Задача 1: Напишите программу, которая бесконечно запрашивает целые числа с консоли. 
//Программа завершается при вводе символа ‘q’ или при вводе числа, сумма цифр которого чётная.

// using System;

// class Program
// {
//     static void Main()
//     {
//         while (true) // Бесконечный цикл
//         {
//             Console.WriteLine("Введите целое число или 'q' для выхода:");
//             string input = Console.ReadLine(); // Чтение строки ввода

//             if (input == "q") // Проверка на ввод 'q'
//             {
//                 break; // Завершение программы
//             }

//             int number;
//             bool isNumberParsed = int.TryParse(input, out number); // Попытка преобразовать строку в число

//             if (!isNumberParsed) // Проверка, было ли успешно преобразовано в число
//             {
//                 Console.WriteLine("Некорректный ввод.");
//                 continue; // Пропуск текущего итерации цикла
//             }

//             int sumOfDigits = 0; // Сумма цифр введенного числа

//             while (number > 0) // Подсчет суммы цифр числа
//             {
//                 sumOfDigits += number % 10; // Добавление последней цифры к сумме
//                 number /= 10; // Удаление последней цифры из числа
//             }

//             if (sumOfDigits % 2 == 0) // Проверка на четность суммы цифр
//             {
//                 Console.WriteLine("Вы ввели число с четной суммой цифр.");
//                 break; // Завершение программы
//             }

//             Console.WriteLine("Продолжаем...");
//         }
//     }
// }


// Задача 2: Задайте массив заполненный случайными трёхзначными числами. 
// Напишите программу, которая покажет количество чётных чисел в массиве.

// using System;
// using System.Linq;

// class Program
// {
//     static void Main()
//     {
//         // Создаем массив размером 10, который будет содержать трехзначные числа
//         int[] numbers = new int[10];

//         // Заполняем массив случайными трехзначными числами
//         for (int i = 0; i < numbers.Length; i++)
//         {
//             // Генерируем случайное число от 100 до 999
//             numbers[i] = Random.Shared.Next(100, 1000);
//         }

//         // Выводим массив в консоль
//         Console.WriteLine(string.Join(", ", numbers));

//         // Объявляем переменную для подсчета количества четных чисел в массиве
//         int evenCount = 0;

//         // Перебираем элементы массива и считаем количество четных чисел
//         foreach (var number in numbers)
//         {
//             // Проверяем, является ли число четным
//             if (number % 2 == 0)
//             {
//                 // Увеличиваем счетчик четных чисел
//                 evenCount++;
//             }
//         }

//         // Выводим результат
//         Console.WriteLine($"Количество четных чисел в массиве: {evenCount}");
//     }
// }


// Задача 3: Напишите программу, которая перевернёт одномерный массив 
// (первый элемент станет последним, второй – предпоследним и т.д.)

using System;

class Program
{
    static void Main()
    {
        // Создаем массив из пяти элементов
        int[] array = {8, 2, 3, 4, 5};
        
        // Переворачиваем массив
        ReverseArray(array);
        
        // Выводим результат
        Console.WriteLine("Перевернутый массив: ");
        foreach (int item in array)
        {
            Console.Write(item + " ");
        }
    }
    
    // Метод для переворачивания массива
    public static void ReverseArray(int[] array)
    {
        // Переворачиваем массив с помощью встроенного метода Reverse
        Array.Reverse(array);
    }
}
